services:
  viewer:
    build:
      context: .
      dockerfile: viewer/Dockerfile
    ports:
      - "4321:4321"
    environment:
      - PAPYRI_UPLOAD_TOKEN=${PAPYRI_UPLOAD_TOKEN:-dev}
    volumes:
      - papyri-data:/data
    restart: unless-stopped

volumes:
  papyri-data:
